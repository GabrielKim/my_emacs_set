(defvar *_defp_facemenu* "facemenu")

(add-to-list 'load-path (concat *_def_libraries_all_path* *_defp_facemenu*))

;;(add-to-list 'load-path "~/.emacs.d/library/facemenu")
(require 'facemenu+)
(require 'face-list)
